---
name: career-planning
description: 基于用户简历、约束条件问答与当前宏观环境（中国五年规划、经济周期如康波周期），生成多年（默认十年）职业规划并渲染为 PDF。当用户要求做职业规划、未来发展方向、多年成长路径时使用；不用于简历投递、ATS 定制或面试准备（那是 career-ops 的职责）。
---

# Career Planning（职业规划）

把"简历 + 个人约束 + 宏观环境"转化为一份结构化、可执行的多年职业规划 PDF。

## 工作流

### 1. 收集输入
- **简历**：用户未给路径时先询问。支持 markdown / txt / 从 PDF/HTML 提取。提取：教育背景、实习经历、项目经历、技能、证书、所在地。
- **约束提问（必做，规划前先问）**：至少问清以下问题，回答必须体现在规划里：
  1. 规划年限（默认 10 年）
  2. 目标方向/岗位（如无，根据简历推荐 1-3 个并让用户确认）
  3. 目标城市与是否接受异地/一线城市
  4. 收入与家庭预期（买房/结婚/生育时间线）
  5. 风险偏好（求稳 vs 搏高收益）与可接受的工作强度
  6. 未来是否考虑读研/考公/考证/创业
  7. 最想补强的 1-2 个能力
  8. 学历提升意愿（如非全日制硕士）与预算

### 2. 能力定位合成
不要复读简历。把散落的经历/技能合成 1-3 句差异化定位（如"数据+业务+财务复合"），并列出 SWOT。只使用简历与用户回答中的真实事实，不虚构量化数据。

### 3. 宏观研究（联网优先，离线兜底）
- **可联网**：搜索最新权威信息——当前五年规划要点（数字经济、数据要素、人工智能+）、经济周期判断（如康波周期阶段）、相关行业趋势。每条结论标注来源与日期。
- **离线**：使用 `references/macro-environment.md`（内置参考，注意其数据日期），并明确告知用户这是内置参考而非实时信息。
- 联网结果用于补充/更新内置参考；两者冲突时以更新的实时信息为准并注明。**禁止编造宏观数据。**

### 4. 撰写规划
按 `references/plan-template.md` 的结构写 `plan.md`（默认 10 年三阶段，年限/城市/方向随用户回答参数化）。
**进阶期与突破期默认各给出 2–3 条可选路线**（除非用户明确要求单一路线）：每条路线写明契合点、前景、关键动作与适配性，并给出推荐路线与最迟选择时间点。

### 5. 渲染 PDF
```bash
python scripts/build_pdf.py plan.md 输出路径/职业规划.pdf
```
脚本自包含：md→HTML→PDF，自动定位本机 Node/Playwright（见脚本头注释）。生成后向用户展示/打开 PDF。

## 硬性规则
- **先提问后规划**：规划必须反映用户对约束问题的回答。
- **康波/经济周期 = 参考框架，非确定性预测**：必须标注判断来源与不确定性。
- **进阶期与突破期默认各给 2–3 条可选路线**：含契合点、前景、关键动作、适配性、推荐与选择时限；用户要求单一化时例外。
- **诚实**：量化数据只允许来自简历真实记录或基于回答的合理推算（推算需标注"预估"）；宏观结论必须有来源。
- **输出语言**：跟随简历/用户语言，默认中文。
- 简历与用户回答冲突时，约束以用户回答为准，但需向用户指出冲突点。
